# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.wrangler
worker/.wrangler

# Rust / Tauri build artifacts (src-tauri/target alone is ~13 GB)
src-tauri/target/

# Tauri's generated permission schemas (rebuilt on every compile)
src-tauri/gen/schemas/

# Local databases — portable-mode runs drop these next to the exe, and
# they contain the user's IGDB/SteamGridDB API keys
*.db
*.db-shm
*.db-wal

# Secrets
.env
.env.*
.dev.vars
*.pem
*.key
*.key.pub
*.priv
minisign.key
*.pfx
deploy.bat
worker/deploy.bat

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
